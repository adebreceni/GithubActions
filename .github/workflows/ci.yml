name: "Test"
on: [push, pull_request, workflow_dispatch]
jobs:
  setup:
    name: "Setup"
    runs-on: ubuntu-20.04
    timeout-minutes: 2
    steps:
      - id: install_deps
        run: |
          sudo apt install ccache tzdata
          cmake --version
          ccache --version
