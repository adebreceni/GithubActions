name: "Test"
on: [push]
jobs:
  setup:
    name: "Setup"
    runs-on: ubuntu-18.04
    container: ubuntu:14.04
    steps:
      - id: setup_env
        run: |
          pwd
          echo $HOME
      - id: build
        run: cmake --version
